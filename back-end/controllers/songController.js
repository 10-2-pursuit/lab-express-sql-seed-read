const express = require('express')


const songs = express.Router()


songs.get('/', (req,res) => {

})
songs.get('/:id', (res,res) => {
    const {id} = re.params
})
songs.post('/', (res,res) => {

})
songs.put('/:id', (res,res) => {
    const {id} = re.params
})
songs.delete('/:id', (res,res) => {
    const {id} = re.params
})